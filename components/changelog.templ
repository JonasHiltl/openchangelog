package components

type ChangelogArgs struct {
	Title    string
	Subtitle string
}

templ Changelog(c ChangelogArgs) {
	<main class="content-container">
		@Header(HeaderArgs(c))
		{ children... }
	</main>
}
