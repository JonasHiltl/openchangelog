package components

type HeaderArgs struct {
	Title    string
	Subtitle string
}

templ Header(args HeaderArgs) {
	<div class="header">
		if args.Title != "" {
			<h1>{ args.Title }</h1>
		}
		if args.Subtitle != "" {
			<p>{ args.Subtitle }</p>
		}
	</div>
}
