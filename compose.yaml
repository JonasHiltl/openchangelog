services:
  openchangelog:
    build: .
    ports:
      - "6001:6001"
    volumes:
      - ./test.db:/data/test.db
      - type: bind
        source: openchangelog.yml
        target: /etc/openchangelog.yml